eureka:
  enabled: true
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    server-url:
      defaultZone: ${EUREKA_SERVER:http://localhost:8761/eureka}
